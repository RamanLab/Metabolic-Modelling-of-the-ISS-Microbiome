qplot(data=Metabolites, x=Metabolites$`Micro-organism`,y=factor(Metabolites$Metabolite,levels = rev(levels(factor(Metabolites$Metabolite)))),fill=factor(Metabolites$Boolean),geom="tile")+scale_fill_manual(values=c("0"="#4df3ce", "1"="#1a96f3",name="Metabolite"))+labs(title="",x="", y="",fill=" ")+theme(axis.text.x = element_text(angle = 30, vjust = 1,size = 12, hjust = 1,face = 'italic',colour='black'),axis.text.y = element_text(vjust = 0.5,size = 12, hjust = 1,colour='black'), axis.title = element_text(face="bold",size=14), plot.title =element_text(size=14,face='bold',hjust=0.5),panel.background = element_blank())+geom_tile(colour="black")+coord_equal()