ggplot(data = Community, aes(y=factor(Community$Site,levels = rev(levels(factor(Community$Site)))), x=factor(Community$`Micro-organism`), fill=Community$MSI)) +geom_tile(colour='black')+coord_equal()+scale_fill_distiller(palette = "Blues",direction=1,name="Metabolic Support Index",na.value =  'grey50',limits=c(0,0.2))+labs(title=" ", x="Micro-organism", y="Site")+theme(axis.text.x = element_text(angle = 30, vjust = 1,size = 12, hjust = 1,face = 'italic',colour = 'black'),axis.text.y = element_text(vjust = 0.5,size = 12, hjust = 1,colour = 'black'),axis.title = element_text(face="bold",size=14),plot.title =element_text(size=14,face='bold',hjust=0.5),panel.background = element_blank(),legend.title=element_text(size = 10, face='bold'),legend.title.align = 1)+geom_text(aes(Community$`Micro-organism`,Site, label=round(Community$MSI,3)), color=ifelse(round(Community$MSI,3)<0.1,'black','white'),size=3)